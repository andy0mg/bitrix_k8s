apiVersion: v1
kind: Service
metadata:
  name: bitrix-push-pub
  namespace: {{ include "bitrix.namespace" . }}
  labels:
    {{- include "bitrix.labels" . | nindent 4 }}
spec:
  selector:
    app: push-pub
  ports:
    - name: http
      port: 80
      targetPort: 8895
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: push-pub
  namespace: {{ include "bitrix.namespace" . }}
  labels:
    {{- include "bitrix.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.push.pub.replicas }}
  selector:
    matchLabels:
      app: push-pub
  template:
    metadata:
      labels:
        app: push-pub
        {{- include "bitrix.labels" . | nindent 8 }}
    spec:
      containers:
        - name: push-pub
          image: {{ .Values.push.image | quote }}
          ports:
            - containerPort: 8895
          env:
            - name: REDIS_ADDR
              value: "redis:6379"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "bitrix.secretName" . }}
                  key: REDIS_PASSWORD
            - name: SECURITY_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "bitrix.secretName" . }}
                  key: PUSH_SECURITY_KEY
            - name: LISTEN_ADDR
              value: "0.0.0.0:8895"
            - name: MODE
              value: "pub"
          readinessProbe:
            tcpSocket:
              port: 8895
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
