---
# NFS + Helm (ingress, nfs provisioner, metrics, cert-manager) + Bitrix chart.
# Вызывается из site.yml после подготовки kubeconfig.

- name: Настроить NFS-сервер на ВМ
  hosts: nfs_server
  become: true
  tags: [nfs]
  roles:
    - nfs_server

- name: Cluster addon’ы (ingress, NFS provisioner, metrics, cert-manager)
  hosts: localhost
  connection: local
  gather_facts: false
  tags: [addons, helm]
  vars:
    repo_root: "{{ repo_root_override | default(playbook_dir | dirname | dirname | dirname) }}"
    kubeconfig_local: "{{ repo_root }}/automation/build/kubeconfig"
  pre_tasks:
    - name: Локальные зависимости (helm, kubectl)
      ansible.builtin.include_tasks: "{{ playbook_dir }}/../tasks/local-cli-prereqs.yml"

    - name: Подгрузить group_vars/all.yml для localhost
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../group_vars/all.yml"
  tasks:
    - name: Проверить kubeconfig
      ansible.builtin.stat:
        path: "{{ kubeconfig_local }}"
      register: _kc

    - name: Сообщить если нет kubeconfig
      ansible.builtin.fail:
        msg: "Нет {{ kubeconfig_local }} — сначала плей с тегами kubeadm,kubeconfig или k3s,kubeconfig / положите kubeconfig вручную."
      when: not _kc.stat.exists

    - name: Каталог automation/build
      ansible.builtin.file:
        path: "{{ repo_root }}/automation/build"
        state: directory
        mode: "0755"

    - name: Helm repo ingress-nginx
      ansible.builtin.shell: helm repo add ingress-nginx {{ ingress_nginx_repo | quote }} 2>&1
      args:
        executable: /bin/bash
      register: _hr1
      failed_when: false
      changed_when: _hr1.rc == 0

    - name: Проверить helm repo ingress-nginx
      ansible.builtin.fail:
        msg: |
          helm repo add ingress-nginx: rc={{ _hr1.rc }}
          {{ _hr1.stdout | default('') }}{{ _hr1.stderr | default('') }}
      when: >
        (_hr1.rc | int != 0)
        and not (
          ((_hr1.stdout | default('')) ~ (_hr1.stderr | default(''))) | lower
          is search('already|exists|added|duplicate|уже')
        )

    - name: Helm repo nfs-subdir-external-provisioner
      ansible.builtin.shell: helm repo add nfs-subdir-external-provisioner {{ nfs_provisioner_repo | quote }} 2>&1
      args:
        executable: /bin/bash
      register: _hr2
      failed_when: false
      changed_when: _hr2.rc == 0

    - name: Проверить helm repo nfs-provisioner
      ansible.builtin.fail:
        msg: |
          helm repo add nfs-subdir: rc={{ _hr2.rc }}
          {{ _hr2.stdout | default('') }}{{ _hr2.stderr | default('') }}
      when: >
        (_hr2.rc | int != 0)
        and not (
          ((_hr2.stdout | default('')) ~ (_hr2.stderr | default(''))) | lower
          is search('already|exists|added|duplicate|уже')
        )

    - name: Helm repo jetstack
      ansible.builtin.shell: helm repo add jetstack https://charts.jetstack.io 2>&1
      args:
        executable: /bin/bash
      register: _hr3
      failed_when: false
      changed_when: _hr3.rc == 0

    - name: Проверить helm repo jetstack
      ansible.builtin.fail:
        msg: |
          helm repo add jetstack: rc={{ _hr3.rc }}
          {{ _hr3.stdout | default('') }}{{ _hr3.stderr | default('') }}
      when: >
        (_hr3.rc | int != 0)
        and not (
          ((_hr3.stdout | default('')) ~ (_hr3.stderr | default(''))) | lower
          is search('already|exists|added|duplicate|уже')
        )

    - name: Helm repo update
      ansible.builtin.command: helm repo update
      changed_when: true

    - name: Шаблон values для ingress-nginx
      ansible.builtin.template:
        src: "{{ playbook_dir }}/../templates/ingress-nginx-values.yaml.j2"
        dest: "{{ repo_root }}/automation/build/ingress-nginx-values.yaml"
        mode: "0644"
      when: install_ingress_nginx | default(true)

    - name: Установить ingress-nginx
      ansible.builtin.command: >
        helm upgrade --install {{ ingress_nginx_release }} {{ ingress_nginx_chart }}
        --kubeconfig {{ kubeconfig_local }}
        --namespace ingress-nginx --create-namespace
        --timeout {{ helm_install_timeout | default('15m') }}
        -f {{ repo_root }}/automation/build/ingress-nginx-values.yaml
      when: install_ingress_nginx | default(true)
      changed_when: true

    - name: Шаблон values для nfs-subdir-external-provisioner
      ansible.builtin.template:
        src: "{{ playbook_dir }}/../templates/nfs-subdir-external-provisioner-values.yaml.j2"
        dest: "{{ repo_root }}/automation/build/nfs-subdir-external-provisioner-values.yaml"
        mode: "0644"
      when: install_nfs_provisioner | default(true)

    - name: Установить nfs-subdir-external-provisioner
      ansible.builtin.command: >
        helm upgrade --install {{ nfs_provisioner_release }} {{ nfs_provisioner_chart }}
        --kubeconfig {{ kubeconfig_local }}
        --timeout {{ helm_install_timeout | default('15m') }}
        --set nfs.server={{ nfs_server_host }}
        --set nfs.path={{ nfs_export_path }}
        --set storageClass.name={{ nfs_provisioner_storage_class }}
        -f {{ repo_root }}/automation/build/nfs-subdir-external-provisioner-values.yaml
      when: install_nfs_provisioner | default(true)
      changed_when: true

    - name: Установить metrics-server (манифест upstream)
      ansible.builtin.command: >
        kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
        --kubeconfig={{ kubeconfig_local }}
      changed_when: true

    - name: Патч metrics-server — tolerations control-plane (одна нода / taint)
      ansible.builtin.command:
        argv:
          - kubectl
          - patch
          - deployment
          - metrics-server
          - -n
          - kube-system
          - --type=strategic
          - --kubeconfig={{ kubeconfig_local }}
          - -p
          - '{"spec":{"template":{"spec":{"tolerations":[{"key":"node-role.kubernetes.io/control-plane","operator":"Exists","effect":"NoSchedule"},{"key":"node-role.kubernetes.io/master","operator":"Exists","effect":"NoSchedule"}]}}}}'
      when: metrics_server_tolerate_control_plane | default(ingress_nginx_on_control_plane | default(true)) | bool
      register: _ms_tol
      changed_when: _ms_tol.rc == 0
      failed_when: false

    - name: Патч metrics-server (insecure kubelet TLS — k3s и типичный kubeadm без kubelet-сертификатов)
      ansible.builtin.command: >
        kubectl patch deployment metrics-server -n kube-system --type=json
        -p='[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--kubelet-insecure-tls"}]'
        --kubeconfig={{ kubeconfig_local }}
      when: metrics_server_patch_kubelet_insecure_tls | default(true)
      register: _msp
      changed_when: "'patched' in (_msp.stdout | default(''))"
      failed_when: false

    - name: Пересоздать поды metrics-server (убрать старый Pending после смены tolerations)
      ansible.builtin.command:
        argv:
          - kubectl
          - delete
          - pods
          - -n
          - kube-system
          - -l
          - k8s-app=metrics-server
          - --kubeconfig={{ kubeconfig_local }}
          - --ignore-not-found
          - --wait=false
      when: metrics_server_tolerate_control_plane | default(ingress_nginx_on_control_plane | default(true)) | bool
      register: _ms_pods_del
      changed_when: "'deleted' in (_ms_pods_del.stdout | default(''))"

    - name: Шаблон values для cert-manager
      ansible.builtin.template:
        src: "{{ playbook_dir }}/../templates/cert-manager-values.yaml.j2"
        dest: "{{ repo_root }}/automation/build/cert-manager-values.yaml"
        mode: "0644"
      when: cert_manager_install | default(true)

    - name: Установить cert-manager
      ansible.builtin.command: >
        helm upgrade --install {{ cert_manager_release }} jetstack/cert-manager
        --kubeconfig {{ kubeconfig_local }}
        --namespace {{ cert_manager_namespace }} --create-namespace
        --timeout {{ helm_install_timeout | default('15m') }}
        --set installCRDs=true
        -f {{ repo_root }}/automation/build/cert-manager-values.yaml
      when: cert_manager_install | default(true)
      changed_when: true

- name: Развернуть Bitrix Helm (секреты + install)
  hosts: localhost
  connection: local
  gather_facts: false
  tags: [bitrix, app]
  vars:
    repo_root: "{{ repo_root_override | default(playbook_dir | dirname | dirname | dirname) }}"
    kubeconfig_local: "{{ repo_root }}/automation/build/kubeconfig"
    helm_chart_dir: "{{ repo_root }}/helm/bitrix"
    secrets_path: "{{ repo_root }}/automation/build/bitrix-secrets.yaml"
    extra_values_path: "{{ repo_root }}/automation/build/bitrix-extra-values.yaml"
  pre_tasks:
    - name: Локальные зависимости (helm, kubectl)
      ansible.builtin.include_tasks: "{{ playbook_dir }}/../tasks/local-cli-prereqs.yml"

    - name: Подгрузить group_vars/all.yml для localhost
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../group_vars/all.yml"
  tasks:
    - name: Проверить kubeconfig
      ansible.builtin.stat:
        path: "{{ kubeconfig_local }}"
      register: _kc2

    - ansible.builtin.fail:
        msg: "Нужен kubeconfig: {{ kubeconfig_local }}"
      when: not _kc2.stat.exists

    - name: Проверить Helm chart (Chart.yaml)
      ansible.builtin.stat:
        path: "{{ helm_chart_dir }}/Chart.yaml"
      register: _bitrix_chart

    - name: Полный репозиторий с helm/bitrix
      ansible.builtin.fail:
        msg: |
          Нет {{ helm_chart_dir }}/Chart.yaml — для bitrix нужен полный клон репозитория, не только automation/.
          На этом хосте: cd {{ repo_root }} && git pull && ls helm/bitrix/Chart.yaml
      when: not _bitrix_chart.stat.exists

    - name: Сгенерировать пароль PostgreSQL
      ansible.builtin.command: openssl rand -base64 24
      register: _pg
      changed_when: false

    - name: Сгенерировать пароль Redis
      ansible.builtin.command: openssl rand -base64 24
      register: _rds
      changed_when: false

    - name: Сгенерировать PUSH_SECURITY_KEY (128 hex-символов)
      ansible.builtin.command: openssl rand -hex 64
      register: _push
      changed_when: false

    - name: Записать bitrix-secrets.yaml
      ansible.builtin.copy:
        dest: "{{ secrets_path }}"
        mode: "0600"
        content: |
          secrets:
            postgresPassword: "{{ _pg.stdout | trim }}"
            redisPassword: "{{ _rds.stdout | trim }}"
            pushSecurityKey: "{{ _push.stdout | trim }}"

    - name: Шаблон доп. values
      ansible.builtin.template:
        src: "{{ playbook_dir }}/../templates/bitrix-extra-values.yaml.j2"
        dest: "{{ extra_values_path }}"
        mode: "0644"

    - name: Пересоздать Postgres STS и PVC после смены storageClass в шаблоне
      ansible.builtin.shell: |
        set -euo pipefail
        export KUBECONFIG="{{ kubeconfig_local }}"
        kubectl delete statefulset postgres -n "{{ bitrix_namespace }}" --ignore-not-found
        kubectl delete pvc postgres-data-postgres-0 sphinx-data -n "{{ bitrix_namespace }}" --ignore-not-found
      args:
        executable: /bin/bash
      when: bitrix_force_storage_reinit | default(false) | bool
      register: _bsi
      changed_when: "'deleted' in ((_bsi.stdout | default('')) ~ (_bsi.stderr | default('')))"

    - name: Helm install / upgrade Bitrix
      ansible.builtin.shell: |
        set -euo pipefail
        chart="{{ repo_root }}/helm/bitrix"
        test -f "$chart/Chart.yaml"
        exec helm upgrade --install "{{ bitrix_release }}" "$chart" \
          --namespace "{{ bitrix_namespace }}" --create-namespace \
          --timeout "{{ helm_install_timeout | default('15m') }}" \
          -f "{{ secrets_path }}" \
          -f "{{ extra_values_path }}"
      args:
        executable: /bin/bash
      environment:
        KUBECONFIG: "{{ kubeconfig_local }}"
      changed_when: true

    - name: Итог
      ansible.builtin.debug:
        msg:
          - "Пароли записаны в {{ secrets_path }} (не коммитьте)."
          - "Откройте https://{{ bitrix_domain }}/bitrixsetup.php после готовности подов."
