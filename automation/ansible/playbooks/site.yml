---
# Полный цикл по умолчанию: Kubernetes (kubeadm) → kubeconfig → NFS → Helm → Bitrix.
# Для k3s: ansible-playbook playbooks/site-k3s.yml (см. также inventory).
#
# Точечно: --tags kubeadm,kubeconfig | nfs | addons | bitrix

- import_playbook: kubeadm-bootstrap.yml

- import_playbook: cluster-common.yml
